[lorecast162-general]
width = 90%
offset-x = 5%
radius = 0.9
separator = |
font-0 = "MesloLGS Nerd Font:size=13;0"

[bar/top]
font-0 = ${lorecast162-general.font-0}
modules-right = fs wired-network wireless-network alsa
modules-center = date
modules-left = logo i3
separator = ${lorecast162-general.separator}
bottom = false
radius = ${lorecast162-general.radius}

[bar/bottom]
font-0 = ${lorecast162-general.font-0}
modules-left = mpd-left
modules-center = mpd-center
modules-right = mpd-right
separator = ${lorecast162-general.separator}
bottom = true

[module/logo]
type = custom/text
content = 

[module/date]
type = internal/date
date = %d/%m/%Y
time = %H:%M:%S

label = %time% %date%
label-foreground = #ffffff

[module/i3]
type = internal/i3
pin-workspaces = true

label-mode = %mode%
label-mode-padding = 2

label-focused = %index% 
label-focused-background = #AA11AA
label-focused-padding = 2

label-unfocused-padding = 1
label-unfocused-background = #555555

label-separator = |
#label-separator-padding = 0.25
label-separator-background = #555555

[module/wired-network]
type = internal/network
interface = eth0
#label-disconnected-foreground = #FF1111
#label-disconnected = : DOWN
label-connected-foreground = #11FF11
label-connected = : UP (%local_ip%) %linkspeed%

[module/wireless-network]
type = internal/network
interface = wlan0
label-disconnected-foreground = #FF1111
label-disconnected = 睊: DOWN
label-connected-foreground = #11FF11
label-connected = 直: %local_ip% on %essid%

[module/alsa]
type = internal/alsa

master-soundcard = hw:0
speaker-soundcard = hw:0
headphone-soundcard = hw:0
master-mixer = PCM

mapped = true

label-volume =  %percentage%
label-muted =  %percentage%

[module/fs]
type = internal/fs

mount-0 = /

format-mounted = <label-mounted>
bar-used-indicator = +
bar-used-width = 10
bar-used-empty = -
bar-used-fill = &

label-mounted = (Free: %free%/%total%)
#label-mounted-foreground = #11FF11
label-unmounted = %mountpoint% (not mounted)
#label-unmounted-foreground = #FF1111

[module/mpd-right]
type = internal/mpd

host = 127.0.0.1
port = 6600

format-online = <icon-prev> <icon-seekb> <icon-stop> <toggle> <icon-seekf> <icon-next> <icon-repeat> <icon-random>
format-playing = ${self.format-online}
format-paused = ${self.format-online}
format-stopped = ${self.format-online}

format-offline = <label-offline>

label-offline = mpd offline!
label-offline-foreground = #FF0000

icon-play = 
icon-toggle = 懶
icon-pause = 
icon-stop = 
icon-prev = 
icon-next = 
icon-seekf = 
icon-seekb = 
icon-random = 列
icon-repeat = 累

[module/mpd-left]
type = internal/mpd

host = 127.0.0.1
port = 6600

format-online = <bar-progress> <label-time> 
format-playing = ${self.format-online}
format-paused = ${self.format-online}
format-stopped = ${self.format-online}

label-song = %artist% - %title%

label-time = %elapsed% / %total%

bar-progress-width = 20
bar-progress-indicator = ▶
bar-progress-fill = ■
bar-progress-empty = ■  
bar-progress-empty-foreground = ${self.bar-progress-empty-background}
bar-progress-empty-background = #000000

[module/mpd-center]
type = internal/mpd

host = 127.0.0.1
port = 6600

format-online = <label-song>
format-playing = 蓼 <label-song>
format-paused = 遼 <label-song>
format-stopped = <label-song>

label-song = %artist% - %title%
